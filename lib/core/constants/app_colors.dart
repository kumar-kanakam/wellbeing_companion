import 'package:flutter/material.dart';

class AppColors {
  // Main Theme
  static const Color background = Color(0xFF0A0A0B); // Pure Obsidian
  static const Color cardColor = Color(0xFF161618);   // Dark Graphite
  
  // Primary Action Color (Gold/Yellow Neon)
  static const Color primary = Color(0xFFFFD700);     // Luxury Gold
  static const Color accent = Color(0xFFE2B007);      // Deep Gold
  
  // Text Colors
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGrey = Color(0xFF8E8E93);
  
  // Glow effect
  static const Color glow = Color(0x40FFD700); 
}